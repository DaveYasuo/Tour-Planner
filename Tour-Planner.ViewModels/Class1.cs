using System;

namespace Tour_Planner.ViewModels
{
    public class Class1
    {
        /*
         * The ViewModels project is fairly easy to understand, as it just contains View Models. You may be wondering why there is a Commands folder inside it.
         * If we were using commands in the old fashioned way, writing a separate class for each command, then we could end up with a great many classes and that would probably warrant putting them into their own project.
         */
    }
}
