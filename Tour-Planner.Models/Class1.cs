namespace Tour_Planner.Models
{
    public class Class1
    {
        /*
         * The DataProviders and Interfaces folders in the second example appear within the Models project, but they have the same responsibilities.
         * Either way, it is through the use of these interfaces that we are able to disconnect the data source and replace it with a mock source of some kind when testing.
         * We will look at an example of this in Chapter 3, Writing Custom Application Frameworks, but for now, let's continue.
         */
    }
}
